doctype html
html
  head
    title Login
    link(rel="stylesheet", href="/css/login.css")
  body
    .login-container
      h2 Login
      form(action="/login", method="post")
        input(type="text", name="username", placeholder="Usuário", required)
        input(type="password", name="password", placeholder="Senha", required)
        button(onclick="window.location='/alunos/lista'") Fazer Login
      button.forgot(type="button", onclick="window.location='/forgot-password'") Esqueceu a senha?